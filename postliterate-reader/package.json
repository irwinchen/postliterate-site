{
  "name": "postliterate-reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "jsdom": "^26.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.3",
    "@mozilla/readability": "^0.5.0"
  }
}
